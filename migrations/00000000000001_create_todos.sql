CREATE TABLE todos (id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY, title TEXT NOT NULL, done BOOLEAN NOT NULL DEFAULT FALSE); ALTER TABLE todos ENABLE ROW LEVEL SECURITY; CREATE POLICY anon_read ON todos FOR SELECT USING (true); GRANT SELECT ON todos TO anon; INSERT INTO todos (title) VALUES ('hello harbor'), ('second item');
